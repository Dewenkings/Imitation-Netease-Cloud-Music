name: Deploy to Development

on:
  push:
    branches: [ develop ]
  pull_request:
    branches: [ develop ]
  workflow_dispatch:
    inputs:
      environment:
        description: 'Deployment environment'
        required: false
        default: 'development'
        type: choice
        options:
        - development
        - testing

jobs:
  deploy:
    uses: ./.github/workflows/deploy.yaml
    with:
      MODE: 'development'
      PORT: 3001
      REMOTE_HOST: ${{ vars.SSH_HOST_DEV }}
      REMOTE_USER_NAME: ${{ vars.SSH_USER_NAME_DEV || 'root' }}
      NAME: 'netease-music-dev'
      INSTANCES: 1
    secrets: inherit